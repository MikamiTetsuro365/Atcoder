#include <iostream>
#include <vector>
#include <algorithm>
#include <random>
#include <string>
#include <cmath>


using namespace std;
typedef long long ll;


int main(){

    string S, T;
    cin >> S >> T;

    //表
    vector<pair<string, string> > SW;
    SW.push_back(make_pair("s", "w"));
    cout << SW.size() << endl;
    
    
    /*
    for(int i = 0; i < S.length(); i++){
        if(S[0] == T[0]){
            continue;
        }else{

        }
    }
    */
}