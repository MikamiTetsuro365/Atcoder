#include "bits/stdc++.h"

using namespace std;
typedef long long int ll;
typedef pair<ll, ll > pi;

int main(){

    ll N; cin >> N;
    // 1 = first -1 = runrun
    ll sum = 0;
    for(ll i = 0; i < N; i++){
        ll t; cin >> t;
        sum += t;
    }

    //if(s)

}