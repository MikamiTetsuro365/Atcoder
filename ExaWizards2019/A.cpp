#include <iostream>
#include <vector>
#include <algorithm>
#include <random>
#include <string>
#include <cmath>
#include <iomanip>
#include <queue>
//greaterで必要
#include <functional>
//重複チェック
#include <set>


using namespace std;
typedef long long ll;
ll N;
ll M;
ll r;
ll INF = 1<<28;


int main() {
    //頂点数
    cin >> N >> M >> r;
    vector<vector<pair<ll, ll> > > G(N);

    ll a, b, t;
    for(ll i = 0; i < M; i++){   
        cin >> a >> b >> t;

    }


    
}