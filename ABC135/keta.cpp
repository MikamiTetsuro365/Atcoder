#include <iostream>
#include <vector>
#include <algorithm>
#include <random>
#include <string>
#include <cmath>
#include <sstream>
#include <istream>
#include <iomanip>
#include <typeinfo>
#include <stack>
#include <map>
 
using namespace std;
typedef long long int ll;

//０以上N以下の整数で、ある条件を満たすものの個数を求めよ

int main(){

    string s;
    cin >> s;

    vector<ll > S(s.length()); 
    
    ll temp = stoi(s);
    for(ll i = 0; i < s.length(); i++){
        S[]
    }

 
} 