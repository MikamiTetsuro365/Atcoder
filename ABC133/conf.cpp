#include <iostream>
#include <vector>
#include <algorithm>
#include <random>
#include <string>
#include <cmath>
#include <sstream>
#include <istream>
#include <iomanip>
#include <typeinfo>
#include <stack>

using namespace std;
typedef long long ll;
ll N;

int main(){
    
    ll L , R;
    cin >> L >> R;

    //cout << L % 2019 << " " << R % 2019 << endl;
    ll cnt = 0;
    vector<ll> md(2019, 1145141919191919);
    vector<pair<ll, ll > > mm;
    for(ll i = L; i <= R; i++){
        for(ll i = L; i <= R; i++){
            
        }        
    }

    sort(mm.begin(), mm.end());
    //cout << mm[0].second << " " << mm[1].second << endl;
    //cout << (mm[0].second * mm[1].second) % 2019 << endl;
} 